
var email = require('../app/js/bundle');

var expect = require('chai').expect;

describe('Karma test', () => {
  	it('should have an element with a class name `emailTo` ', () => {

  		//var rx = new email.Email();
    	//expect( rx.multiply(2, 4) ).equal(8); 
  	});
});